// Ablation #12 (Summary.md): depth-dependent eps_tail(d) vs the global value.
// Placeholder values — replace with the per-depth quantiles from
// scripts/calibrate_tail_divergence.py (summary.per_horizon[m].eps_tail_by_depth).
{
  gear+: {
    eps_tail: 0.2,
    eps_tail_by_depth: { '0': 0.3, '1': 0.2, '2': 0.1 },
  },
}
